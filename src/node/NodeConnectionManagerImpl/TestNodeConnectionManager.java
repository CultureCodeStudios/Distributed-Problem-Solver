package node.NodeConnectionManagerImpl;

import node.NodeConnectionManager;

//TODO this may be ignored because we may not be using the same connection logic. This logic is in the Test node currently.
public class TestNodeConnectionManager implements NodeConnectionManager {

}
